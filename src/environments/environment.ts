export const environment = {
    production: false,
    // Cambia a 'false' cuando quieras conectar con el backend real
    useMocks: true,
    apiUrl: 'http://localhost:3000/api', // URL base del backend
  };
  